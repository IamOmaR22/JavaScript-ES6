// Need to write export to access this from here to other file. We can now use this in any file because of export.

export const add = (a, b) => a+b;  

export const sub = (a, b) => a-b;

export const times = (a, b) => a*b;

export const div = (a, b) => a/b;

export const mod = (a, b) => a%b;  // module